/* eslint-env jest */

import { shallow } from 'enzyme'
import Nav from 'components/Nav'

describe('COMPONENT -> Nav...', () => {
  let nav = shallow(<Nav />)

  it(`no tests yet...`, () => {
    expect(nav)
  })
})
