/* eslint-env jest */

import { shallow } from 'enzyme'
import Nav from './'

describe('COMPONENT -> Title...', () => {
  let nav = shallow(<Nav />)

  it(`no tests yet...`, () => {
    expect(nav)
  })
})
